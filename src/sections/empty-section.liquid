{% schema %}
{
  "name": "Template",
  "settings": [
    {
      "type": "header",
      "content": "No Settings",
      "info": "Shopify requires at least one section be added to a template by default so this is a shell section with no setttings. Use the \"Add section\" in the sidebar to add sections to the page."
    }
  ],
  "templates": ["page"]
}
{% endschema %}
