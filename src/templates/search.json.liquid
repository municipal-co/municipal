{% layout none %}

[
{%- if search.performed -%}
    {%- if search.results_count > 0 -%}
    {% for item in search.results %}
        {% render 'product-json' with product: item %}
        {%- unless forloop.last -%}
          ,
        {%- endunless -%}
      {% endfor %}
    {%- endif -%}
  {%- endif -%}
]
